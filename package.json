{
  "name": "government-watchdog-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Government Watchdog frontend — reviewer-internal/local Alpine timeline skeleton (Stage 1 Slice 4 A).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:smoke": "vitest run test/integration-smoke.test.ts"
  },
  "devDependencies": {
    "jsdom": "^24.1.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}

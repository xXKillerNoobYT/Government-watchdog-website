node_modules
dist
.vite
.artifact
.git
.paperclip
.playwright-mcp
verification-artifacts
harness
docs/evidence
# issue-evidence screenshots at the repo root (public/ imagery stays in context)
/gov*.png
.env
.env.*
# Private-runtime archives must never enter a Docker build context or remote
# builder cache. The removed prebuilt-artifact flow is intentionally unsupported.
.artifact-local
gw-web-artifact-*.tar.gz
gw-private-runtime-*.tar.gz
